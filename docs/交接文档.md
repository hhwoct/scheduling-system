# 排班系统 交接文档

> 更新日期：2026-08-27（星期四）
> 仓库：https://github.com/hhwoct/scheduling-system.git
> 分支：`main`（仅此一条分支；本地领先 origin/main 的 68 个提交尚未推送，见 §2/§13）
> 适用读者：接手开发 / 部署 / 联调 / 运维的后继人员

---

## 1. 项目概况

- **名称**：排班系统 MVP（案例门店：昆明滚滚）
- **形态**：管理端 Web 单页应用 + 员工自助端（同一前端按角色路由分流）
- **技术栈**
  - 后端：.NET 10.0 Web API（Minimal API，集中在 `Program.cs`，共 **82 个端点**）+ EF Core + Pomelo MySQL
  - 数据库：MySQL（`shift_mvp` 库）
  - 前端：Vue 3 + Element Plus + Vite 6 + Pinia + Vue Router + ECharts + axios + dayjs
  - 认证：JWT Bearer（令牌有效期 24 小时）+ 角色授权策略（`AdminOnly` / `SystemAdminOnly`）；前端 15 分钟无操作自动退出登录（`stores/auth.js` 空闲监听）
  - 测试：xUnit，当前 **235 个用例全部通过**（2026-08-27 实测 ~20s；覆盖算法/应用服务/安全/异常中间件/基础设施，测试数据层用 EF Core InMemory + SQLite）；另有接口冒烟 34 项 + 浏览器 E2E 56 项全绿（见 `docs/全量测试报告.md`）
- **人员规模**：33 名员工（23 全职 + 10 兼职 E101~E110），25 个登录账号（admin + E001/manager + E002~E023）；**兼职 E101~E110 无登录账号**（仅排班数据）
- **文档现状**：
  - `README.md` / `docs/接口文档.md` —— 内容随各轮提交同步（接口文档已收 08-25 排班调整范围接口，但头部版本仍标 v1.1、偏好/调整明细接口待补，见 §9/§10）；**`docs/功能清单.md` 已滞后**（仍写旧 OTP 流程与 186 用例，缺 08-24/25 新功能条目），待同步（见 §10）
  - 测试类文档：`docs/测试计划.md`（手工测试计划 MTP-2026-08）、`docs/测试报告.md`（单元测试报告）、`docs/集成测试报告.md`（API 级端到端）、`docs/全量测试报告.md`（08-27：单测 235 + 冒烟 34 + E2E 56 全绿）、`docs/代码审查报告-2026-08-18.md`
  - 设计文档：`docs/排班调整交互与偏好学习设计.md`（08-24 排班调整 P0~P2 与偏好学习方案）
  - 根目录还有需求文档/开发文档/MVP 开发流程（.docx / .md，早期产物）

---

## 2. 当前仓库状态

**2026-08-27 状态：origin/main 仍停在 `32b98ee`（2026-08-19 18:02）；本地 `main` 领先 68 个提交，尚未推送（见 §13）。**

- 08-19 之后本地累计新增 **68 个提交**，主线工作：
  - 08-24：排班偏好学习（店长习惯学习 feature 分支合并，默认关闭）+ 排班调整交互 P0~P2（撤销条/发布前调整摘要/调整明细落库/需求联动建议/复制上周/批量改休/偏好角标）+ 侧边栏收起 + 界面样式修复
  - 08-25：排班调整范围操作接口（add-slot/remove-slot/replace-slot/move-range/clear-range）+ 取消发布 + 自助修改密码（安全审查 P1-1）+ 忘记密码改「姓名+手机号」+ 员工初始密码统一 `工号@123456` + 第三/四轮全项目审查修复（234 测试全绿）+ 店长工作站文档同步
  - 08-27：日明细员工泳道对齐 + 满员行加人 + read-all 通知口径修复 + 全量测试（234 单测+34 冒烟+56 E2E）+ 06:00 幽灵时段修复（235 测试全绿）
- 工作区仅剩少量文档改动（本文件头部、测试计划/全量测试报告执行人行），随本轮一并处理。
- `scripts/itest-*.json` 为集成测试运行产物（内含 dev JWT），已加入 `.gitignore`，**不入库**。
- **接手后第一件事：审阅本地 68 个提交并推送到 origin/main，然后按 §4 环境准备跑通本地后再继续开发。**

## 3. 目录结构

```text
排班系统/
  backend/
    ShiftScheduling.Api/           Web API 主项目（.NET 10）
      Program.cs                   全部 82 个端点 + 中间件/限流/认证注册（大文件，谨慎修改）
      Algorithm/                   排班引擎（纯算法，与 EF 解耦，可单测）
        SchedulingEngine.cs        引擎编排（四阶段；输入净化剔除闭店空窗 06:00~12:30 需求）
        Steps/                     RestDayAllocator / ShiftAllocator / WorkstationAllocator
        BreakAllocator.cs          阶段四：班中休息分配
        SchedulingModels.cs        算法输入/输出 record 定义
        SchedulingTimeHelper.cs    时段/跨天工具
      Application/                 按领域分层：Service + DTO + 接口
        Auth/ Employees/ EmployeeSkills/ Workstations/ ShiftTemplates/ RuleConfigs/
        PeakHours/ Schedules/ StaffingRequirements/ Ai/ Preferences/ ★  Security/  Common/
      Infrastructure/              EF DbContext + 实体映射、JwtTokenService、审计日志、限流等
    ShiftScheduling.Api.Tests/     xUnit 测试（235 用例：Algorithm/Application/Common/Infrastructure/Security/TestInfra；EF InMemory + SQLite）
  frontend/                        Vue 3 管理端 + 员工端
    src/api/                       按领域拆分的 axios 请求封装（peakHours/staffingRequirements/skillMatrix/ai/preferences 等）
    src/views/                     23 个页面组件（含人数需求/技能矩阵/AI/高峰时段/偏好学习页面）
    src/router/index.js            路由 + 角色守卫
    vite.config.js                 5173 端口，/api 代理到 5059
  database/
    init_shift_mvp.sql             建库建表 + 模拟数据（已吸收 20260824 及之前迁移，见 §5）
    migrations/                    39 个增量迁移（按文件名日期顺序执行）
    backups/                       备份
  scripts/                        集成测试运行产物（itest-*.json，已 gitignore，不入库）
  docs/                            文档（交接/功能清单/接口文档/安全审查/代码审查报告/测试计划/测试报告/集成测试报告/全量测试报告/排班调整交互与偏好学习设计）
```

---

## 4. 环境与启动

### 4.1 本机实测环境（2026-08-27）

| 组件 | 版本/状态 | 备注 |
|---|---|---|
| OS | macOS（Apple Silicon） | |
| .NET SDK | 10.0.302 | |
| MySQL | 9.6.0（Homebrew，本机自装） | README 写的是 8.0；代码里 `MySqlServerVersion(8.0.36)` 兼容声明，实测可用 |
| Node.js | 18+ | |
| 本地 `shift_mvp` 库 | 已建，**已应用全部迁移（39 个，含 20260825~20260830 系列）** | 店长工作站（BOSS/S10）、偏好学习、调整明细、通知索引、手机号统一均已就位 |
| 后端测试 | **235/235 通过** | `dotnet test` 实测 ~20s（08-27 全量测试后，含 06:00 回归测试） |
| 接口冒烟 + E2E | 34 + 56 全绿 | `scripts/smoke-test-modules.sh` + 无头浏览器 E2E（08-27，见 docs/全量测试报告.md） |
| 前端构建 | `frontend/dist/` 已于 08-27 14:45 构建 | 含人数需求/技能矩阵/AI/偏好学习页面产物 |

### 4.2 ★ 配置方式（敏感配置不入库，README 已同步）

- **`backend/ShiftScheduling.Api/appsettings.Development.json` 已从仓库删除**，并加入 `.gitignore`（敏感配置不入库）。
- 现在配置通过以下任一方式提供（本地当前用 **User Secrets**）：

```bash
# 方式 A：User Secrets（本机已配置，UserSecretsId = ce03a1e6-88d0-4de1-b133-2ef0dddc1b6a）
cd backend/ShiftScheduling.Api
dotnet user-secrets set "ConnectionStrings:ShiftMvp" "Server=localhost;Port=3306;Database=shift_mvp;User=root;Password=root123;Charset=utf8mb4;"
dotnet user-secrets set "Jwt:Issuer" "ShiftScheduling.Api"
dotnet user-secrets set "Jwt:Audience" "ShiftScheduling.Admin"
dotnet user-secrets set "Jwt:SigningKey" "<至少32字节的密钥>"

# 方式 B：环境变量
export ConnectionStrings__ShiftMvp="Server=localhost;Port=3306;Database=shift_mvp;User=root;Password=root123;Charset=utf8mb4;"
export Jwt__SigningKey="<至少32字节>"

# 方式 C：本地自建 appsettings.Development.json（已被 gitignore，不会提交）
```

- 启动时校验：连接串为空 / SigningKey < 32 字节 / ExpireMinutes 不在 5~1440 会直接抛异常并提示（`Program.cs` 顶部）。

### 4.3 启动步骤

```bash
# 1. 数据库（新环境需 init + 按序执行全部 migrations，见 §5）
mysql -u root -p < database/init_shift_mvp.sql
# 然后按文件名顺序执行 database/migrations/*.sql

# 2. 后端（http://localhost:5059）
cd backend/ShiftScheduling.Api && dotnet run

# 3. 前端（http://localhost:5173，/api 代理到 5059）
cd frontend && npm install && npm run dev

# 4. 测试与健康检查
cd backend/ShiftScheduling.Api.Tests && dotnet test
curl http://localhost:5059/api/health
```

### 4.4 测试账号与密码约定

| 用户名 | 角色 | 初始密码约定 |
|---|---|---|
| `admin` | SYSTEM_ADMIN（可改排班规则） | `Admin@123456` |
| `manager` / `E001` | STORE_MANAGER（规则只读） | `Manager@123456`（manager）/ 员工规则 |
| `E002`~`E023` | 全职员工 | `工号@123456`（如 E002@123456；2026-08-25 起统一约定，见 `6dfce40` 与 `migrations/20260811_fix_password_hashes.sql` 注释） |

> 兼职 E101~E110 未创建登录账号（只有员工档案与技能数据，参与排班但不登录系统）。

> `init_shift_mvp.sql` 出于安全考虑不再内置真实密码哈希（占位哈希不可登录），也不预插 admin/manager 账号；按序执行迁移时 `20260811_fix_password_hashes.sql` 会重置为初始密码约定（admin=Admin@123456、manager=Manager@123456、员工=工号@123456），首次登录后请立即修改。

> 密码全部为 bcrypt（work factor 12）哈希存储，明文约定记录在 `20260811_fix_password_hashes.sql` 注释中。若账号登录不上，可用「忘记密码」流程（员工姓名+注册手机号，直接重置无验证码；每 IP 15 分钟限 3 次，失败锁定按姓名维度）自助重置，或登录后通过右上角下拉「修改密码」改密。

---

## 5. 数据库与迁移

- **初始化**：`database/init_shift_mvp.sql`（建库建表 + 模拟数据，33 员工、需求、班次模板等）。
- **★ 注意**：`init_shift_mvp.sql` 是基线，**已吸收 20260824 及之前全部迁移**（含 `peak_restricted_hours` 表、`schedule_summaries` 休息列、`early_returned`、周期唯一键等）；新环境 init 后**按文件名日期顺序执行 `database/migrations/` 下 20260825 起的迁移（共 39 个，均已幂等）**。老库升级路径：不执行 init，按序执行全部迁移。
- 迁移清单（按序）：

| 迁移 | 内容 |
|---|---|
| 20260806_harden_user_passwords.sql | 密码哈希加固 work factor 12 |
| 20260806_add_september_date_params.sql | 9 月日期参数 |
| 20260807_add_employee_accounts.sql | 员工登录账号 |
| 20260807_add_leave_requests.sql / add_shift_swaps.sql | 请假 / 换班表 |
| 20260811_add_password_version.sql / add_rule_version.sql | password_version / 规则版本列 |
| 20260811_concurrency_constraints.sql / fix_p2_data_model.sql / fix_password_hashes.sql | 并发唯一索引 / P2 数据模型 / 密码修正 |
| 20260812_add_low_skill_workstation.sql | 低技能工作站标记 |
| 20260813_add_parttime_employees.sql | 10 名兼职 E101~E110 |
| 20260814_fix_admin_staffing_2200.sql / fix_e023_skills.sql | 行政岗 22:00 需求归零 / E023 技能 |
| 20260817_add_break_and_peak_hours.sql | 休息字段 + 高峰禁休时段表（见下） |
| 20260818_add_weekend_staffing.sql | 周末日期类型 + 三档人数需求（见下） |
| 20260819_add_ideal_count.sql | 人数需求「最少/最好」两档（见下） |
| 20260820_add_ai_config.sql | AI 文档识别配置表（DeepSeek，见下） |
| 20260821_add_staffing_remark.sql | 人数需求格子备注（框选批量修改可附带，见下） |
| 20260822_add_employee_generalist.sql | 员工「通岗」标记（楼面低技能岗位自动 ≥3 分，见下） |
| **20260819_fix_workday_staffing.sql** ★ | **本次新增：回填 WORKDAY 平日人数需求（旧 init 无平日档，见下）** |
| 20260823_add_schedule_period_unique.sql / add_shift_swaps_target_index.sql / fix_holiday_mark_20260927.sql | 排班周期唯一约束 / 换班目标索引 / 2026-09-27 中秋调休补班标记修正 |
| 20260824_add_cleaning_shift.sql / add_leave_early_return.sql / add_min_daily_work_hours.sql / fix_s5_cleaning_mapping.sql | S9 保洁班 / 请假提前返岗列 / 每日最低工时规则 / S5 不再覆盖保洁岗 |
| **20260825_add_boss_workstation.sql** ★ | **店长工作站(BOSS)+店长班 S10(19:00-04:00)，E001 专职化，行政岗凌晨/深夜需求清零（见下）** |
| **20260825_add_boss_backup.sql** ★ | **E002/E003 店长站辅助技能（4 分），店长休息日轮替顶班；S10 优先级=1** |
| **20260825_add_admin_station_skill.sql** ★ | **E006 管理岗技能 3→4 分（店长专职后补强管理岗承接）** |
| **20260826_add_preference_learning.sql** ★ | **偏好学习数据底座：`employee_preferences`（员工×班次/工作站/休息×day_type 认可频次，由发布计划聚合）+ `preference_trends` 趋势表 + `preference_weight` 规则（默认 0.3，0=关闭）（见下）** |
| **20260827_add_schedule_adjustments.sql** ★ | **排班调整明细表 `schedule_adjustments`：店长每步修改的 before/after 结构化落库（见下）** |
| **20260827_fix_0600_out_of_axis.sql** ★ | **06:00 幽灵时段修复：清零保洁岗打烊时段误配需求 + 清理 72 条幽灵明细/7 条陈旧缺口/2 条空壳汇总（见下）** |
| 20260828_add_preference_learning_weight.sql | 偏好权重规则改名 `preference_weight` → `preference_learning_weight`，语义改 0-1 连续权重（0=关闭，>0=启用） |
| 20260830_add_notification_index.sql / align_ideal_count_check.sql / fix_leave_type_check.sql / unify_employee_phones.sql | 通知未读复合索引 / ideal_count CHECK 双路径对齐（归正 36 条存量违规）/ 请假类型白名单 CHECK / 老库升级路径全员手机号统一 12312341234（数据层审查修复配套） |

### 20260817 迁移内容

1. `schedule_summaries` 新增 4 列：`break_start_time`、`break_end_time`（time）、`break_cover_employee_id`（bigint，NULL=无人顶岗）、`break_workstation_id`（bigint，休息时主工作站）。
2. 新表 `peak_restricted_hours`：`id / store_id / start_time（含）/ end_time（不含）/ status(1启用 0停用) / created_at / updated_at`，含 store 外键与索引。
3. 为所有现有门店插入默认高峰时段 **20:00-22:00**。
4. 写入一条 `MIGRATE_BREAK_PEAK_HOURS` 审计日志。

> 本机 `shift_mvp` 库已应用该迁移（2026-08-17 12:15），接手者无需重复执行；部署到新环境时必须执行。

### 20260818 迁移内容（人数需求三档化）

1. 日期类型三档化：`date_parameters.day_type` 支持 **WORKDAY / WEEKEND / HOLIDAY**。业务口径：平日 = 周一~周四+周日，**周末 = 周五+周六（晚市高峰日）**，节假日 = 法定节假日（2026-09-25/26 中秋）。
2. 重新划分历史日期：周五/周六改判 `WEEKEND`，周日改判 `WORKDAY`，中秋 09-25/26 标记 `is_legal_holiday=1`、09-27 调休补班。
3. 为每个门店复制 HOLIDAY 人数需求作为 `WEEKEND` 初始值（幂等，可重复执行）。
4. 配套新功能：管理端「基础数据 → 人数需求」页面（矩阵编辑 + Excel 导入/模板导出 + 跨日期类型复制），后端 `GET/PUT /api/staffing-requirements`。

### 20260819 迁移内容（人数需求两档化）

1. `staffing_requirements` 新增 `ideal_count`（最好人数）列，并回填 = `required_count`（最少人数）。
2. 语义：单元格单数字 N = 最少且最好 N；`(M,N)`（页面与 Excel 均支持） = 最少 M、最好 N。
3. 排班算法：硬性满足最少人数（低于即缺口），人手有余时通过软性阶段尽量补足最好人数（`ShiftAllocator` 两阶段填充 + `WorkstationAllocator` 2A+ 软性参考分配），软性缺口不影响发布与合理度（合理度仍按最少人数计算）。
4. 前端页面改为甘特图式编辑：行=工作站、列=半小时时段，**时间轴为营业时段 12:00 至次日 06:00**（00:00 起标注「次日」，06:00-11:30 闭店时段隐藏但数据保留），颜色深浅=最少人数、斜纹=有最好目标；点击单元格编辑，按住拖动框选区域后弹出批量修改面板（最少/最好 + 快捷键，应用到选区）。
5. 营业日口径（20260819 追加）：**一个营业日 = 当天 12:00 至次日 06:00**；排班算法中，某日历日 00:00-05:30 的凌晨时段按**前一天**的日期类型取人数需求（如周日 02:00 按周六=周末档），合理度计算同步按此口径。
6. 需求统计与按需排班（20260819 追加）：
   - 人数需求页实时统计每档的最少/最好人·时、峰值并发（含三档合计）；
   - 生成排班页展示周期需求概览（`GET /api/staffing-requirements/preview`）；
   - 排班引擎：模板班次排完后仍有最少人数缺口时，按连续缺口块自动生成**临时班次 D1/D2…**（起止时间跟随需求、可跨午夜，落库 `shift_template_id=NULL`，月/周视图显示班次代码「班」），生成结果返回需求/覆盖/缺口人·时与覆盖率（`GenerateScheduleResult` 新增 6 个字段）。

### 20260820 迁移内容（AI 文档识别）

1. 新表 `ai_configs`（store_id / provider / api_key / base_url / model / status），按门店保存 DeepSeek API Key，**Key 明文存库、接口仅返回掩码**。
2. 后端新增 `Application/Ai/`：`AiConfigService`（配置读写/掩码）+ `DocumentAiService`（调用 DeepSeek `/chat/completions`，`response_format=json_object`，temperature=0；支持表格文本与图片 base64 两种输入；返回条目逐条校验：日期类型/工作站匹配/30 分钟对齐/人数范围）。
3. 端点（AdminOnly）：`GET/PUT /api/ai/config`、`POST /api/ai/test`（连通性测试）、`POST /api/ai/parse-requirement-doc`（文档识别）。
4. 前端「人数需求」页：新增「AI 识别导入」（Excel/图片均可）与「AI 设置」对话框（Key/接口地址/模型 + 测试连接）；识别结果并入甘特图编辑器，仍需点「保存」才写库。
5. 注意：DeepSeek 公开 API 的文本模型（deepseek-chat）支持 JSON 输出；图片识别是否可用取决于所用模型是否支持视觉输入（默认 deepseek-chat 不支持图片时会返回 400 错误，页面上会给出明确提示）。

### 20260821 迁移内容（需求格子备注）

1. `staffing_requirements` 新增 `remark VARCHAR(200) NULL` 列（可空）。
2. 后端：`StaffingRequirementItem/Entry` 增加 `Remark`，保存时校验最长 200 字符、空白转为 NULL。
3. 前端：单格编辑与拖动框选批量修改面板均可填写备注；有备注的格子右上角显示橙色小点，悬停 tooltip 显示「最少 X 人｜备注：…」；批量应用时留空 = 清除选区备注。

### 20260822 迁移内容（员工通岗）

1. `employees` 新增 `is_generalist TINYINT`（是否通岗）。
2. 通岗语义：大部分楼面工作都能做（传送/保洁/咨客/服务等 `is_low_skill=1` 岗位）。开启时自动为这些岗位写入技能分 **max(现有, 3)**；取消时清 0（技能行保留）；仅全职员工可设置。
3. 技能等级总览页新增「通岗」开关列（姓名旁显示绿色「通」标签）；后端端点 `PUT /api/skill-matrix/generalist`（AdminOnly：管理员+店长）。
4. 通岗写入的是真实 `employee_skills` 行，排班算法无需改动即可使用。

### 20260819_fix_workday_staffing 迁移内容（★ 本次新增）

1. 背景：旧版 init 仅配置 HOLIDAY（含周末）需求；20260818 拆分 WEEKEND 时只复制了 HOLIDAY→WEEKEND，导致 WORKDAY（周一~周四+周日）全 0、按需排班时无岗位安排。
2. 内容：按 init 业务口径无条件重算 WORKDAY 全部时段（行政岗 13:00-22:00=1；厨房 18:00-23:30=2、00:00-03:00=1；服务/传送 19:00-23:30=2、00:00-04:00=1；咨客/客户经理 19:00-23:30=1；内/外吧 18:30-23:30=1、00:00-04:00=1；保洁 21:00-23:30 或 00:00-06:00=1；其余时段 0），`required_count` 与 `ideal_count` 同步回填。
3. 幂等：UPDATE 无条件按口径重算，重复执行结果一致；审计日志按 action_type+remark 去重。

### 20260825 迁移内容（★ 店长工作站，本轮新增）

1. **背景**：店长(E001)原按「管理岗」排班——管理岗唯一班次 S1 为 13:00-22:00，且历史数据残留管理岗凌晨 00:00-02:00 / 深夜 22:00-23:30 需求=1（与 20260819 口径矛盾），导致店长被排 13:00 上班 + 凌晨/深夜残差补班，单日工时达 13.5h。
2. `add_boss_workstation.sql`：新增工作站「店长」(code=BOSS)、班次「店长班」S10（19:00-次日04:00 跨天）、店长站需求（19:00-23:30 + 00:00-03:30 = 1，三档 day_type）；E001 主岗位改「店长」、技能重置为仅店长站 5 分主技能；行政岗（管理/文员仓管/采购/工程/网络）凌晨与 22:00 起需求清零（对齐 20260819 口径）。
3. `add_boss_backup.sql`：E002/E003 配店长站 4 分辅助技能（店长 5 分主技能保持优先），店长休息日由副手轮替顶店长班；S10 优先级=1（**算法侧** `ShiftAllocator.ShiftPriorityMap` 同步加入 S10——仅改模板 priority 不生效，见 §6）。
4. `add_admin_station_skill.sql`：E006 管理岗技能 3→4 分（店长专职后补强管理岗承接）。
5. **已知限制**：店长休息日 19:00-20:00 管理岗仍有结构性缺口（副手 19:00 顶店长班、E006 19:00-20:00 在采购岗），WARN 级不阻塞发布，由店长人工安排；碎片块（< 6.5h）全职不接补班（避免副手顶班日 12h→15h 超长工时，权衡记录于 `ResidualGapFiller.cs`）。
6. 全部迁移幂等（NOT EXISTS / ON DUPLICATE / 条件 UPDATE 守卫），两种安装路径均可安全执行。

### 20260826 / 20260827 迁移内容（★ 偏好学习 + 排班调整明细）

1. **20260826**：新表 `employee_preferences`（store_id / employee_id / day_type / shift_code / workstation_id / freq / last_seen_at，唯一键 store+员工+类型+班次+岗位）——以「店长手动调整后发布的排班」为认可样本，按已发布计划的 `schedule_summaries` 聚合员工 ×（班次/工作站/休息）× day_type 的认可频次（休息日样本：shift_code/workstation_id 均为 NULL）；`preference_trends` 记录每期贴合率/覆盖率趋势；规则 `preference_weight` 默认 0.3（0=关闭学习）。由 `PreferenceService.RebuildAsync` 全量重建（幂等、自愈）。
2. **20260827**：新表 `schedule_adjustments`（plan_id / employee_id / work_date / time_slot / action_type / before_json / after_json / operator_user_id / operator_name / created_at，含 plan 与 store 外键+索引）——店长在 move-segment / day-status / slot-status / adjust 端点的每步修改结构化落库（与业务同事务），用于发布前可追溯、纠错方向学习与需求联动建议。
3. 权重规则改名（20260828）：`preference_weight` → `preference_learning_weight`，语义改 **0-1 连续权重**——排序分 = 技能分×(1−w) + 偏好归一化分×w，w=1 完全按偏好；默认置 0（关闭），数值越大越倾向店长习惯。

### 20260827_fix_0600_out_of_axis 迁移内容（★ 06:00 幽灵时段修复）

1. **根因**：人数需求中保洁岗误配 `06:00 required_count=1`（打烊后时段），引擎按缺口生成 D 临时班次 → 每个计划每天多 1 条 `time_slot='06:00:00'`、超出日明细时间轴（13:00~次日 05:30）的**界面不可见幽灵明细**。
2. 迁移内容（幂等）：清零 3 行 `06:00` 误配需求；删除历史 72 条幽灵明细、7 条陈旧缺口、2 条空壳汇总（start=06:00/end=06:30/0.5h）。
3. **引擎侧同步加防御**：`SchedulingEngine.Execute` 输入净化剔除打烊时段（06:00~12:30）需求，防止误配复发；新增回归测试 `ClosedMorningDemandTests`（235 测试全绿）。

---

## 6. 后端架构与排班引擎

### 6.1 分层

- **端点层**：Minimal API 全部写在 `Program.cs`（82 个端点），统一 `ApiResponse` 信封、全局异常中间件、JWT 认证、按端点限流（登录 20 次/分、重置 15 分钟 3 次）。
- **应用层**：`Application/<领域>/` 下 `IXxxService` + `XxxService` + `XxxDtos`，业务事务内写审计日志（`IAuditLogService`，与业务同事务）。
- **算法层**：`Algorithm/` 纯 C# 类，输入/输出为 record（`SchedulingModels.cs`），不依赖 EF，可用内存数据单测。
- **持久层**：`ShiftSchedulingDbContext` + `Entities/`（snake_case 列映射）。

### 6.2 排班引擎四阶段（`SchedulingEngine.GenerateAsync`）

| 阶段 | 组件 | 职责 |
|---|---|---|
| 1 | `RestDayAllocator` | 休息日分配（月休按周期折算，连续工作达上限强制打断） |
| 2 | `ShiftAllocator` | 班次分配（全职优先，请假过滤；最少人数硬性填充 + 最好人数软性填充两阶段，模板班次不足时按连续缺口块自动生成临时班次 D1/D2…） |
| 3 | `WorkstationAllocator` | 工作站分配（2A-2G + 2A+ 软性参考：缺口块检测/块状借调/二次填补/逐段兜底，每人每天借调上限 2 次；兼职仅低技能岗位） |
| 4 ★ | `BreakAllocator` | **班中休息分配（本次新增）** |
| 4.5 ★ | `ResidualGapFiller` | **剩余缺口兜底填充（本轮修复）：全职优先接整块（≥每日最低工时），兼职接碎片块；缺口块与员工已有班次部分重叠时按最长可接连续子段裁剪补班（原整块拒绝致缺口虚高）** |

> **S10 店长班优先级**：`ShiftAllocator.ShiftPriorityMap` 硬编码 `S10=1`（先于 S1 等行政班分配，保证店长休息日副手先被店长班占用）；**前端班次管理页 S10 的「优先级」输入已禁用**（界面修改不生效）。map 未收录的班次按 `int.MaxValue` 排最后，仅改模板 priority 无效。

> **输入净化（71e8595）**：`SchedulingEngine.Execute` 第一步剔除**闭店空窗（06:00~12:30）**的需求，防止误配打烊时段需求生成界面不可见的幽灵班次（历史根因：保洁岗 06:00 误配 required=1 → 每计划每天 1 条 06:00 D 班次，已由迁移 `20260827_fix_0600_out_of_axis` 清理存量 + 回归测试封闭，见 §5）。

### 6.3 ★ 班中休息规则（阶段四，`BreakAllocator.cs`）

- 每次休息固定 **30 分钟**；班次时长 **< 4 小时不安排休息**；上班 2 小时内、下班 1 小时内禁休。
- **休息不得与高峰禁休时段重叠**（无配置时算法兜底默认 20:00-22:00）。
- 选槽优先级：休息后本岗冗余最大 → 离高峰边界越远越好 → 该槽休息人数最少（分散休息）。
- 冗余不足时**借调顶岗**：候选人需当班覆盖该时段 + 有该站技能 + 该时段原分配在其他站 + 离开原岗位后原站不缺人；主技能优先、技能分高者优先；**每人每天最多借调 2 次**。
- 不熟练借调输出 `BREAK_BORROW_INEXPERIENCED`（INFO）；无人顶岗输出 `BREAK_UNCOVERED`（WARN，店长人工处理）；高峰把整个可休窗口挤没时休息被迫安排在窗口起点并告警。
- **休息照算工时**（不扣减 WorkHours）。跨午夜班次结束时间按 24 小时制回绕。

### 6.4 ★ 高峰禁休时段（`PeakHours/`）

- 每门店可配置多条（启用状态上限 **10 条**），时间 **30 分钟对齐**、开始早于结束、启用区间不可重叠。
- 接口：`GET/POST/PUT/DELETE /api/peak-restricted-hours`（`AdminOnly`），增删改全部写审计日志。
- 前端管理页 `/peak-hours`（`PeakHoursView.vue`），菜单项「高峰时段」。

### 6.5 合理度（rationality）

- `/schedules/{planId}/rationality`：按真实覆盖 ÷ 需求计算（最少人数口径）；**无人顶岗的班中休息时段覆盖人数 -1**（有借调顶岗的不扣减），支持跨午夜回绕判断。
- 08-19 修复后合理度由 87% 提升至 97%：D 班次按岗位分块、借调防拆墙、支持不重叠补班（`5a56605`）。

### 6.6 AI 文档识别（`Application/Ai/`）

- `AiConfigService`：按门店保存 DeepSeek 配置（API Key 明文存库、接口仅返回掩码）；`DocumentAiService`：调用 DeepSeek `/chat/completions`（json_object、temperature=0），支持表格文本与图片 base64 输入，结果逐条校验（日期类型/工作站匹配/30 分钟对齐/人数范围）。
- 端点（AdminOnly）：`GET/PUT /api/ai/config`、`POST /api/ai/test`（连通性）、`POST /api/ai/parse-requirement-doc`（识别）。
- 注意：deepseek-chat 不支持图片输入，`kind=image` 会返回 400，前端页面有明确提示。

### 6.7 ★ 排班调整与偏好学习（08-24~25 新增，`Application/Preferences/` + `Schedules/`）

- **调整明细落库**：店长在日明细的每步修改（加人/换人/平移/改休/取消排班）结构化写入 `schedule_adjustments`（before/after JSON、操作类型、操作人），与业务数据同事务（写入点：move-segment / day-status / slot-status / adjust 端点）。
- **撤销**：前端撤销条（5 秒）+ Ctrl+Z / Cmd+Z；平移撤销按新时段重放、跨计划调整清空撤销栈、批量模式与撤销互斥、撤销分段恢复。
- **发布前调整摘要**：`GET /schedules/{planId}/adjustment-summary` 对比生成快照与当前差异，店长发布前可见系统将学习什么。
- **范围操作接口**（仅草稿）：`PUT add-slot`（空位加人，已发布也可用）、`remove-slot`（撤回，已发布自动通知员工）、`replace-slot`（范围换人）、`move-range`（±30 分钟平移，跨轴/重叠校验 400）、`clear-range`（取消排班）；已发布计划仅空位加人/撤加人可用。
- **取消发布**：`POST /schedules/{planId}/unpublish` 退回草稿并通知员工（原子抢占防并发重复）。
- **偏好学习（默认关闭）**：`preference_learning_weight=0` 起默认关闭；语义为 **0-1 连续权重**——排序分 = 技能分×(1−w) + 偏好归一化分×w，w=1 完全按偏好。调整明细加权进入偏好聚合（纠错方向学习）；休息偏好、店长调整量统计、兼职人员语义修正均已处理；`POST /preferences/rebuild` 全量重建（幂等、自愈）。
- **需求联动建议**：`GET /schedules/{planId}/demand-insights` —— 反复手动补人的时段提示调整人数需求配置。
- 管理端「偏好学习」页（`/preferences`，`PreferencesView.vue` + `api/preferences.js`）：统计卡/矩阵/趋势展示。

---

## 7. 前端

### 7.1 路由（`src/router/index.js`）

- 登录 `/login`；404 兜底。
- 员工端 `/employee/*`（`EmployeeLayout`）：`schedule`（我的班表）、`leave`、`swap`、`notifications`。
- 管理端 `/`（`MainLayout`）：`dashboard`、`employees`、`workstations`、`shift-templates`、`rules`、`peak-hours`（高峰时段）、`staffing-requirements`（人数需求）、`skill-matrix`（技能等级总览）、`schedules/generate`、`schedules/view`、`reports`、`audit-logs`、`preferences`（偏好学习）、`leave-review`、`swap-review`、`notifications`。
- 路由守卫按角色分流（EMPLOYEE 只能进员工端；STORE_MANAGER/SYSTEM_ADMIN 进管理端）。

### 7.2 API 封装（`src/api/`）

`auth / employee(s) / leave / notifications / peakHours / staffingRequirements / skillMatrix / ai / preferences / rules / schedules / shiftTemplates / store / swap / workstations`，统一走 `utils/request`（axios 实例，401 自动清 token 跳登录）。

### 7.3 ★ 本次前端改动

- 新增「高峰时段」管理页（`PeakHoursView.vue` + `api/peakHours.js`），菜单与路由已挂。
- 日视图/月视图矩阵：班中休息员工灰色标记「休」，显示「顶班 XXX」或「无人顶岗」，支持跨午夜；月视图日历增加「N 人班中休」统计。
- 周视图甘特图：显示休息时段与顶岗人姓名。
- 员工端我的班表：新增「休息」列（含顶岗/无人顶岗提示）与「**我顶岗的记录**」区块（时间/顶岗岗位/替谁顶岗）。
- 发布排班问题列表已并入休息相关告警（`BREAK_UNCOVERED` / `BREAK_BORROW_INEXPERIENCED`）。
- 08-18/19 新增「人数需求」甘特图编辑器（三档日期类型、最少/最好两档、框选批量修改、格子备注、Excel 导入导出、类型复制、周期统计、AI 识别导入）与「技能等级总览」页（色块编辑 + 通岗开关）。
- 日明细交互 v3：**单击**高亮该员工当天全部色块（点空白清除）、**双击**切换休息/上班、按住**拖动**可移动半小时工作段（吸附 30 分钟格子并高亮落点）。
- 日明细交互 v4：工作站行内**员工泳道对齐**——同一员工当天的色块固定在同一横向行（全职在前、兼职在后，各按工号排序），未在场的时段留空占位，避免色块随在场人数上下跳动；工具条新增「**加人**」按钮（满员/有人时段也可加人，新员工与现有人员**并存**，弹窗提示在岗人次，撤销条/Ctrl+Z 可回退）。
- 生成排班页修复周期需求概览首次进入不加载（watch immediate）；管理端侧栏「员工端（我的班表）」入口固定到侧栏底部。
- 08-24/25「排班调整交互」：日明细空格子点击加人、按住滑动选择多个连续时段（跨午夜）、满员行加人并存、范围换人/左右平移/按人改休/取消排班（均仅草稿，撤销条 + Ctrl+Z 可回退）；已发布计划支持空位加人/撤加人并自动通知员工；「取消发布」退回草稿；发布前调整摘要面板。
- 08-24/25「偏好学习」：新增「偏好学习」页（`PreferencesView.vue`：统计卡/矩阵/趋势），偏好角标、批量改休、复制上周、需求联动建议（反复手动补人的时段提示调整人数需求）；默认关闭（权重=0）。
- 08-27：管理端「一键已读」口径修复（`822afa3`）——只处理门店级 + 发给当前管理员本人的通知，不再连带员工个人通知；header 红点与列表/未读数一致。

---

## 8. 近期版本变更速览（08-17 ~ 08-27）

| 日期 | 提交 | 内容 |
|---|---|---|
| 08-17 | （已并入 419c577） | 班中休息（阶段四）+ 高峰禁休时段 + 安全加固（password_version 令牌失效/IP 限流/OTP 不进响应体/手机号脱敏/换班审批并发原子化） |
| 08-18 | 419c577 | 功能增强：周末需求、理想人数两档、通岗、技能矩阵总览、用人需求页、AI 识别 |
| 08-18 | 3b4ab2d/1f9e670/5cf7266/7a2fe32 | 第二轮代码审查修复（backend/frontend/database/chore） |
| 08-19 | 7a3cb96/5a56605/a6b0a7e/c73e88f/2001f6f/ee84167/382447b/ad964ce/e59c865 | 第三轮审查修复 + 引擎修复（选站丢员工、合理度 87%→97%、需求上限、OTP 绑定手机号、空值守卫、审计入事务等） |
| 08-19 | 本轮提交 | 测试套件扩展至 186 用例 + SQLite/InMemory 基建 + 手机号校验 + 前端交互修复 + 20260819_fix_workday_staffing + 文档同步 |
| 08-24 | 19023b6/e3dfbc8/58a8b9c/84cf0d9 等 | 排班偏好学习（店长习惯学习 feature 分支合并）+ 排班调整交互 P0~P2（撤销/发布前摘要/调整明细落库/需求联动建议/复制上周/批量改休/偏好角标）+ 侧边栏可收起 + 界面样式修复 |
| 08-25 | afb5aca/cb0690f/6dfce40/b52a43b/92d69c1/eb5cdd8 等 | 自助修改密码（P1-1）+ 忘记密码改姓名+手机号 + 员工初始密码 `工号@123456` + 范围操作接口（add/remove/replace/move/clear）+ 取消发布 + 第三/四轮全项目审查修复（234 测试全绿）+ 店长工作站文档同步 |
| 08-27 | c2f66c6/71e8595/822afa3/c7f7d24/1d07bd1 | 全量测试（234 单测+34 冒烟+56 E2E）+ 06:00 幽灵时段修复（235 全绿）+ read-all 口径修复 + 日明细泳道对齐 + 满员行加人 |

## 9. 接口速查（详见 docs/接口文档.md v1.1）

当前共 **82 个端点**。相对早期版本新增：

- 高峰禁休时段：`GET/POST/PUT/DELETE /api/peak-restricted-hours`
- 人数需求：`GET/PUT /api/staffing-requirements`、`GET /api/staffing-requirements/preview`
- AI：`GET/PUT /api/ai/config`、`POST /api/ai/test`、`POST /api/ai/parse-requirement-doc`
- 技能矩阵：`GET /api/skill-matrix`、`PUT /api/skill-matrix/cell`、`PUT /api/skill-matrix/generalist`
- 排班调整细分：`PUT /api/schedules/{planId}/day-status`、`/move-segment`、`/slot-status`
- 字段级：月/周/日视图与 `/employee/my-schedule` 透出休息时段与顶岗信息（含 `Covers[]` 我顶岗记录）；登录/me 返回 `passwordVersion`；员工手机号脱敏；生成结果返回需求/覆盖/缺口人·时与覆盖率（`demandMinHours/demandIdealHours/coveredHours/gapHours/coveragePct/demandShiftCount`）

**08-20 ~ 08-27 新增端点**：

- 认证：`POST /api/auth/change-password`（自助改密，password_version+1 旧 Token 全失效，写审计）
- 排班调整范围操作（见 §6.7）：`PUT /api/schedules/{planId}/add-slot`、`/remove-slot`、`/replace-slot`、`/move-range`、`/clear-range`、`GET /add-candidates`、`POST /unpublish`、`POST /copy-previous`、`GET /adjustment-summary`、`GET /adjustments`、`GET /demand-insights`
- 偏好学习：`GET /api/preferences/stats|matrix|top|trends`、`POST /api/preferences/rebuild`
- 请假：`PUT /api/leave-requests/{id}/early-return`（请假期间提前返岗）

> `docs/接口文档.md` 已收录改密/取消发布/范围操作接口，但**偏好学习与调整明细/需求联动/复制上周接口尚未录入**，头部版本仍为 v1.1，待升版（见 §10）。

## 10. 交接待办清单（08-27 状态）

- [x] ~~提交并推送未提交改动~~ —— 08-19 已完成（§2/§13）
- [x] ~~更新 README / 功能清单 / 接口文档~~ —— 历史同步已完成（截至 c7f7d24）
- [x] ~~新增交接文档~~ —— 本文件持续维护
- [ ] **推送到 origin/main** —— 本地领先 68 个提交（08-19 后未推送），接手后先审阅推送（§2/§13）
- [ ] `docs/功能清单.md` 同步：仍含旧「密码重置 OTP」流程（已改「姓名+手机号」双验证）与 186 用例数字，且缺 08-24/25 排班调整/偏好学习/取消发布/自助改密等条目
- [ ] `docs/接口文档.md` 升版 v1.2：补偏好学习/调整明细/需求联动/复制上周接口，更新头部版本号与日期
- [ ] `frontend/.env.example` 的 `VITE_API_TARGET=http://localhost:5000` 与默认 5059 不一致，建议改 5059
- [ ] 根目录杂物清理：`review-20260811.json` / `review-full.json` / `review-report.json` / `review-zh.json` 共 4 个 0 字节 json（`1` 已清理）
- [ ] 忘记密码为「姓名+手机号」直接重置（无短信/OTP 通道）；演示环境全员手机号统一为 12312341234，生产上线前必须配置唯一真实手机号并考虑短信验证
- [ ] 默认密码 `工号@123456` 不含小写字母，与改密/忘记密码策略「大写+小写+数字」不一致（提示性问题，见 docs/全量测试报告.md P2）
- [ ] 店长访问审计日志页无「无权限」提示（API 已 403、页面 0 行无泄露，纯 UX，见 docs/全量测试报告.md P2）
- [ ] `scripts/itest-*.json` 已 gitignore（含 dev JWT）；后续跑集成测试如产出新状态文件记得清理

## 11. 已知问题与注意事项

- **BREAK_UNCOVERED 告警需人工**：高峰禁休、短班、借调不满足时算法只告警不强制解决，发布前店长需处理（发布页问题列表可见）。
- **高峰时段默认值**：算法无配置时兜底 20:00-22:00；门店配置被全部停用/删除后同样回退默认值。
- **休息照算工时**：员工周工时统计不受 30 分钟休息影响（产品决策，勿改）。
- **借调上限**：工作站借调与休息顶岗各自独立限制每人每天 2 次。
- **MySQL 版本**：代码声明 ServerVersion 8.0.36，本机 MySQL 9.6 实测正常；如遇 Pomelo 兼容问题，优先对齐 8.0。
- **`Program.cs` 是单文件 1800+ 行**：改端点建议按既有分区注释定位；后续可考虑迁移到控制器或按领域拆分，但当前勿做无谓重构。
- **多租户**：所有查询必须按 `StoreId` 过滤（本次已在通知工号查询处补漏）；换班审批使用原子 `ExecuteUpdate`，修改时保持。
- **`database/backups/` 含历史明文密码备份**（`shift_mvp_before_init_20260806_154731.sql` 中可见 `admin123_dev_plaintext_replace_later`），已被 .gitignore 排除，勿提交、勿外传。
- **AI 图片识别**：deepseek-chat 不支持图片输入，`kind=image` 会返回 400；换用支持视觉的模型后 `DocumentAiService` 无需改动。
- **WORKDAY 回填口径硬编码**在 `20260819_fix_workday_staffing.sql` 中；若门店工作站结构大改，需同步调整该迁移或人工配置。
- **通岗开关**写的是真实 `employee_skills` 行（楼面低技能岗位 max(现有,3)），取消时清 0，技能行保留。
- **06:00 幽灵时段（已修复，71e8595）**：引擎输入净化剔除闭店空窗（06:00~12:30）需求，迁移已清理存量；若经 Excel 导入误写该时段需求，引擎会忽略、界面不可见但数据保留，注意保持需求只落在 12:00~次日 06:00 时间轴内。
- **通知口径（822afa3）**：管理端「一键已读」只处理门店级 + 发给当前管理员本人的通知，不再连带员工个人通知——修改时保持该口径。
- **默认密码与策略不一致（P2）**：初始密码 `工号@123456` 无小写字母，无法通过改密/忘记密码表单设置回该值，属提示性不一致，非安全漏洞（见 docs/全量测试报告.md）。
- **审计页权限提示缺失（P2 UX）**：店长打开 `/audit-logs` 只见空页，无「无权限」提示（API 本身 403 正确，无数据泄露）。
- **偏好学习默认关闭**：`preference_learning_weight=0`；开启前建议先跑 `POST /api/preferences/rebuild` 重建聚合，并知悉排序分将按 0-1 权重混合技能分与偏好分。

---

## 12. 常用命令速查

```bash
# 后端
cd backend/ShiftScheduling.Api && dotnet run            # http://localhost:5059
cd backend/ShiftScheduling.Api.Tests && dotnet test     # 235 个用例（实测 ~20s）
curl http://localhost:5059/api/health

# 前端
cd frontend && npm install && npm run dev               # http://localhost:5173
npm run build                                           # 产物 frontend/dist/

# 数据库
mysql -u root -p < database/init_shift_mvp.sql
mysql -u root -p shift_mvp < database/migrations/20260817_add_break_and_peak_hours.sql

# 配置（User Secrets）
cd backend/ShiftScheduling.Api
dotnet user-secrets list
dotnet user-secrets set "ConnectionStrings:ShiftMvp" "..."

# 登录接口验证
curl -X POST http://localhost:5059/api/auth/login -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"Admin@123456"}'
```

---



## 13. 历史里程碑（git log 摘要）

- `e59c865`（08-19 14:19）第三轮审查——审计入事务与并发重复键兜底 —— **推送前 HEAD（本轮提交在其之上）**
- 08-19：引擎修复（选站丢员工 / 合理度 87%→97% / 需求上限 / OTP 绑定手机号）+ 第三轮审查修复
- 08-18：`419c577` 功能增强（周末需求/理想人数/通岗/技能矩阵/用人需求/AI 识别）+ 第二轮审查修复 + `14954cb` 文档更新
- 08-17：班中休息（阶段四）+ 高峰禁休时段 + 安全加固
- 08-14 及更早：`0eda671` 全职优先排班、兼职「兼」标记、合理度图 ECharts 重写、并发唯一约束、请假/换班、init 基线（详见 git log）

**2026-08-19（本轮）**：测试套件扩展至 186 用例 + WORKDAY 需求回填迁移 + 文档同步，全部提交并推送 origin/main。

**2026-08-25（本轮）**：店长工作站（BOSS/S10/E001 专职/副手顶班）+ 排班算法修复（残差段式裁剪）+ 第四轮审查修复（手机号脱敏回环/FORBIDDEN 403/超管配置化/次日缺口）+ 测试套件修复（60d4c6c 起无法编译，修复后 190 用例全绿），提交 `1f5572c`。

**2026-08-25（本轮·续）**：自助修改密码（`afb5aca`，安全审查 P1-1）+ 忘记密码改「姓名+手机号」双验证（`cb0690f`）+ 员工初始密码统一 `工号@123456`（`6dfce40`）+ 排班调整范围接口 add/remove/replace-slot、move/clear-range（`4d0d59f`~`92d69c1`）+ 取消发布（`eb5cdd8`）+ 第三/四轮全项目审查修复（`f7d77bb`/`eb5c1ec`，234 测试全绿 + E2E）。

**2026-08-27（本轮）**：日明细泳道对齐（`c7f7d24`）+ 满员行加人（`1d07bd1`）+ read-all 口径修复（`822afa3`）+ 全量测试报告（234 单测+34 冒烟+56 E2E，`c2f66c6`）+ **06:00 幽灵时段修复**（引擎输入净化 + 迁移 `20260827_fix_0600_out_of_axis`，新增回归测试 235 全绿，`71e8595` / `1f4b459`）。
# 排班系统 交接文档

> 更新日期：2026-08-17（星期一）
> 交接人：hhw ｜ 仓库：https://github.com/hhwoct/scheduling-system.git
> 分支：`main`（仅此一条分支，本地与 origin/main 均停在 `0eda671`）
> 适用读者：接手开发 / 部署 / 联调 / 运维的后继人员

---

## 1. 项目概况

- **名称**：排班系统 MVP（案例门店：昆明滚滚）
- **形态**：管理端 Web 单页应用 + 员工自助端（同一前端按角色路由分流）
- **技术栈**
  - 后端：.NET 10.0 Web API（Minimal API，集中在 `Program.cs`，共 **53 个端点**）+ EF Core + Pomelo MySQL
  - 数据库：MySQL（`shift_mvp` 库）
  - 前端：Vue 3 + Element Plus + Vite 6 + Pinia + Vue Router + ECharts + axios + dayjs
  - 认证：JWT Bearer（30 分钟有效期）+ 角色授权策略（`AdminOnly` / `SystemAdminOnly`）
  - 测试：xUnit，当前 **14 个用例全部通过**（2026-08-17 实测）
- **人员规模**：33 名员工（23 全职 + 10 兼职 E101~E110），25 个登录账号（admin + E001/manager + E002~E023）；**兼职 E101~E110 无登录账号**（仅排班数据）
- **文档现状**：
  - `README.md` —— **已过时**（仍描述"仓库内置 appsettings.Development.json"，未包含新功能与高峰时段接口，见 §10 待办）
  - `docs/功能清单.md`（2026-08-13）、`docs/接口文档.md` v1.0（2026-08-13）—— 均**未包含本次班中休息与高峰禁休功能**
  - 根目录还有需求文档/开发文档/MVP 开发流程（.docx / .md，早期产物）

---

## 2. ⚠️ 当前仓库状态（接手第一件事）

**本地工作区有一大批未提交改动，这是本次交接的核心内容。**

- 本地 `main` 与 `origin/main` 一致，均停在提交 `0eda671`（2026-08-14，"全职优先排班 + 补齐赵保洁E023技能"）。
- 工作区有 **24 个已跟踪文件改动（含 1 个删除）+ 7 个未跟踪路径（9 个新文件，另加本交接文档）**，约 +527/-82 行，功能改动全部属于 **「班中休息 + 高峰禁休时段 + 安全加固」** 三项工作，尚未提交。
- **接手后第一件事：阅读本文档，确认本地联调通过后 commit & push**（建议按功能拆成 2~3 个提交）。

### 未提交改动总览

| 分类 | 文件 | 说明 |
|---|---|---|
| 排班算法 | `Algorithm/BreakAllocator.cs`（新）、`Algorithm/SchedulingEngine.cs`、`Algorithm/SchedulingModels.cs` | 新增阶段四：班中休息分配 |
| 高峰时段 | `Application/PeakHours/`（新，3 个文件）、`Infrastructure/Persistence/Entities/PeakRestrictedHourEntity.cs`（新）、`ShiftSchedulingDbContext.cs` | 高峰禁休时段 CRUD |
| 数据库 | `database/migrations/20260817_add_break_and_peak_hours.sql`（新） | 休息字段 + 高峰时段表（见 §5） |
| 认证/安全 | `Program.cs`、`Application/Auth/*`、`Infrastructure/JwtTokenService.cs`、`Application/Employees/EmployeeService.cs` | password_version 令牌失效、登录 IP 限流、OTP 不出响应体、手机号脱敏（见 §8.3） |
| 排班业务 | `Application/Schedules/ScheduleDtos.cs`、`ScheduleService.cs`、`Entities/ScheduleSummaryEntity.cs` | 月/周/日视图与员工端班表透出休息字段；换班审批并发原子化；合理度计入无人顶岗休息扣减 |
| 配置 | `.gitignore`（+1 行）、**删除 `appsettings.Development.json`** | 敏感配置移出仓库（见 §4.2） |
| 前端 | `router/index.js`、`layout/MainLayout.vue`、`views/PeakHoursView.vue`（新）、`api/peakHours.js`（新）、`views/{Daily,Month,Week,Schedule,EmployeeSchedule}View.vue` | 高峰时段页面 + 各视图班中休息/顶岗展示 |
| 测试 | `Tests/Algorithm/BreakAllocatorTests.cs`（新）、`SchedulingEngineTests.cs`、`Security/JwtTokenServiceTests.cs` | 新增 3 个休息分配用例 |

---

## 3. 目录结构

```text
排班系统/
  backend/
    ShiftScheduling.Api/           Web API 主项目（.NET 10）
      Program.cs                   全部 53 个端点 + 中间件/限流/认证注册（大文件，谨慎修改）
      Algorithm/                   排班引擎（纯算法，与 EF 解耦，可单测）
        SchedulingEngine.cs        引擎编排（四阶段）
        Steps/                     RestDayAllocator / ShiftAllocator / WorkstationAllocator
        BreakAllocator.cs          ★ 阶段四：班中休息分配（本次新增）
        SchedulingModels.cs        算法输入/输出 record 定义
        SchedulingTimeHelper.cs    时段/跨天工具
      Application/                 按领域分层：Service + DTO + 接口
        Auth/ Employees/ EmployeeSkills/ Workstations/ ShiftTemplates/
        RuleConfigs/ PeakHours/ ★   Schedules/ Security/
      Infrastructure/              EF DbContext + 实体映射、JwtTokenService、审计日志、限流等
    ShiftScheduling.Api.Tests/     xUnit 单元测试（Algorithm / Security）
  frontend/                        Vue 3 管理端 + 员工端
    src/api/                       按领域拆分的 axios 请求封装（含新 peakHours.js）
    src/views/                     20 个页面组件（含新 PeakHoursView.vue）
    src/router/index.js            路由 + 角色守卫
    vite.config.js                 5173 端口，/api 代理到 5059
  database/
    init_shift_mvp.sql             建库建表 + 模拟数据（★不含新表，见 §5）
    migrations/                    15 个增量迁移（按文件名日期顺序执行）
    backups/                       备份
  docs/                            文档（本文件 + 功能清单 + 接口文档 + 安全审查报告）
```

---

## 4. 环境与启动

### 4.1 本机实测环境（2026-08-17）

| 组件 | 版本/状态 | 备注 |
|---|---|---|
| OS | macOS（Apple Silicon） | |
| .NET SDK | 10.0.302 | |
| MySQL | 9.6.0（Homebrew，本机自装） | README 写的是 8.0；代码里 `MySqlServerVersion(8.0.36)` 兼容声明，实测可用 |
| Node.js | 18+ | |
| 本地 `shift_mvp` 库 | 已建，**已应用全部迁移（含 20260817）** | `peak_restricted_hours` 已有默认记录 20:00-22:00 |
| 后端测试 | 14/14 通过 | `dotnet test` 实测 947ms |
| 前端构建 | `frontend/dist/` 已于 08-17 12:20 构建 | 含 PeakHoursView 产物 |

### 4.2 ★ 配置方式已变更（README 已过时）

- **`backend/ShiftScheduling.Api/appsettings.Development.json` 已从仓库删除**，并加入 `.gitignore`（敏感配置不入库）。
- 现在配置通过以下任一方式提供（本地当前用 **User Secrets**）：

```bash
# 方式 A：User Secrets（本机已配置，UserSecretsId = ce03a1e6-88d0-4de1-b133-2ef0dddc1b6a）
cd backend/ShiftScheduling.Api
dotnet user-secrets set "ConnectionStrings:ShiftMvp" "Server=localhost;Port=3306;Database=shift_mvp;User=root;Password=root123;Charset=utf8mb4;"
dotnet user-secrets set "Jwt:Issuer" "ShiftScheduling.Api"
dotnet user-secrets set "Jwt:Audience" "ShiftScheduling.Admin"
dotnet user-secrets set "Jwt:SigningKey" "<至少32字节的密钥>"

# 方式 B：环境变量
export ConnectionStrings__ShiftMvp="Server=localhost;Port=3306;Database=shift_mvp;User=root;Password=root123;Charset=utf8mb4;"
export Jwt__SigningKey="<至少32字节>"

# 方式 C：本地自建 appsettings.Development.json（已被 gitignore，不会提交）
```

- 启动时校验：连接串为空 / SigningKey < 32 字节 / ExpireMinutes 不在 5~1440 会直接抛异常并提示（`Program.cs` 顶部）。

### 4.3 启动步骤

```bash
# 1. 数据库（新环境需 init + 按序执行全部 migrations，见 §5）
mysql -u root -p < database/init_shift_mvp.sql
# 然后按文件名顺序执行 database/migrations/*.sql

# 2. 后端（http://localhost:5059）
cd backend/ShiftScheduling.Api && dotnet run

# 3. 前端（http://localhost:5173，/api 代理到 5059）
cd frontend && npm install && npm run dev

# 4. 测试与健康检查
cd backend/ShiftScheduling.Api.Tests && dotnet test
curl http://localhost:5059/api/health
```

### 4.4 测试账号与密码约定

| 用户名 | 角色 | 初始密码约定 |
|---|---|---|
| `admin` | SYSTEM_ADMIN（可改排班规则） | `Admin@123456` |
| `manager` / `E001` | STORE_MANAGER（规则只读） | `Manager@123456`（manager）/ 员工规则 |
| `E002`~`E023` | 全职员工 | 工号本身（历史约定，见 `migrations/20260811_fix_password_hashes.sql` 注释） |

> 兼职 E101~E110 未创建登录账号（只有员工档案与技能数据，参与排班但不登录系统）。

> 密码全部为 bcrypt（work factor 12）哈希存储，明文约定记录在 `20260811_fix_password_hashes.sql` 注释中。若账号登录不上，可用「忘记密码」流程（用户名+手机号+OTP）重置，**OTP 验证码只打印在后端日志**（非生产环境，`[DEV-OTP]` 前缀）。

---

## 5. 数据库与迁移

- **初始化**：`database/init_shift_mvp.sql`（建库建表 + 模拟数据，33 员工、需求、班次模板等）。
- **★ 注意**：`init_shift_mvp.sql` 是基线，**不含** `peak_restricted_hours` 表与 `schedule_summaries` 休息列；新环境必须 init 后**按文件名日期顺序执行 `database/migrations/` 下全部 15 个迁移**。
- 迁移清单（按序）：

| 迁移 | 内容 |
|---|---|
| 20260806_harden_user_passwords.sql | 密码哈希加固 work factor 12 |
| 20260806_add_september_date_params.sql | 9 月日期参数 |
| 20260807_add_employee_accounts.sql | 员工登录账号 |
| 20260807_add_leave_requests.sql / add_shift_swaps.sql | 请假 / 换班表 |
| 20260811_add_password_version.sql / add_rule_version.sql | password_version / 规则版本列 |
| 20260811_concurrency_constraints.sql / fix_p2_data_model.sql / fix_password_hashes.sql | 并发唯一索引 / P2 数据模型 / 密码修正 |
| 20260812_add_low_skill_workstation.sql | 低技能工作站标记 |
| 20260813_add_parttime_employees.sql | 10 名兼职 E101~E110 |
| 20260814_fix_admin_staffing_2200.sql / fix_e023_skills.sql | 行政岗 22:00 需求归零 / E023 技能 |
| **20260817_add_break_and_peak_hours.sql** ★ | **本次新增（见下）** |

### 20260817 迁移内容

1. `schedule_summaries` 新增 4 列：`break_start_time`、`break_end_time`（time）、`break_cover_employee_id`（bigint，NULL=无人顶岗）、`break_workstation_id`（bigint，休息时主工作站）。
2. 新表 `peak_restricted_hours`：`id / store_id / start_time（含）/ end_time（不含）/ status(1启用 0停用) / created_at / updated_at`，含 store 外键与索引。
3. 为所有现有门店插入默认高峰时段 **20:00-22:00**。
4. 写入一条 `MIGRATE_BREAK_PEAK_HOURS` 审计日志。

> 本机 `shift_mvp` 库已应用该迁移（2026-08-17 12:15），接手者无需重复执行；部署到新环境时必须执行。

---

## 6. 后端架构与排班引擎

### 6.1 分层

- **端点层**：Minimal API 全部写在 `Program.cs`（53 个端点），统一 `ApiResponse` 信封、全局异常中间件、JWT 认证、按端点限流（登录 5 次/分、重置 OTP 15 分钟 3 次）。
- **应用层**：`Application/<领域>/` 下 `IXxxService` + `XxxService` + `XxxDtos`，业务事务内写审计日志（`IAuditLogService`，与业务同事务）。
- **算法层**：`Algorithm/` 纯 C# 类，输入/输出为 record（`SchedulingModels.cs`），不依赖 EF，可用内存数据单测。
- **持久层**：`ShiftSchedulingDbContext` + `Entities/`（snake_case 列映射）。

### 6.2 排班引擎四阶段（`SchedulingEngine.GenerateAsync`）

| 阶段 | 组件 | 职责 |
|---|---|---|
| 1 | `RestDayAllocator` | 休息日分配（月休按周期折算，连续工作达上限强制打断） |
| 2 | `ShiftAllocator` | 班次分配（全职优先，请假过滤） |
| 3 | `WorkstationAllocator` | 工作站分配（2A-2G：缺口块检测/块状借调/二次填补/逐段兜底，每人每天借调上限 2 次；兼职仅低技能岗位） |
| 4 ★ | `BreakAllocator` | **班中休息分配（本次新增）** |

### 6.3 ★ 班中休息规则（阶段四，`BreakAllocator.cs`）

- 每次休息固定 **30 分钟**；班次时长 **< 4 小时不安排休息**；上班 2 小时内、下班 1 小时内禁休。
- **休息不得与高峰禁休时段重叠**（无配置时算法兜底默认 20:00-22:00）。
- 选槽优先级：休息后本岗冗余最大 → 离高峰边界越远越好 → 该槽休息人数最少（分散休息）。
- 冗余不足时**借调顶岗**：候选人需当班覆盖该时段 + 有该站技能 + 该时段原分配在其他站 + 离开原岗位后原站不缺人；主技能优先、技能分高者优先；**每人每天最多借调 2 次**。
- 不熟练借调输出 `BREAK_BORROW_INEXPERIENCED`（INFO）；无人顶岗输出 `BREAK_UNCOVERED`（WARN，店长人工处理）；高峰把整个可休窗口挤没时休息被迫安排在窗口起点并告警。
- **休息照算工时**（不扣减 WorkHours）。跨午夜班次结束时间按 24 小时制回绕。

### 6.4 ★ 高峰禁休时段（`PeakHours/`）

- 每门店可配置多条（启用状态上限 **10 条**），时间 **30 分钟对齐**、开始早于结束、启用区间不可重叠。
- 接口：`GET/POST/PUT/DELETE /api/peak-restricted-hours`（`AdminOnly`），增删改全部写审计日志。
- 前端管理页 `/peak-hours`（`PeakHoursView.vue`），菜单项「高峰时段」。

### 6.5 合理度（rationality）

- `/schedules/{planId}/rationality`：按真实覆盖 ÷ 需求计算；**无人顶岗的班中休息时段覆盖人数 -1**（有借调顶岗的不扣减），支持跨午夜回绕判断。

---

## 7. 前端

### 7.1 路由（`src/router/index.js`）

- 登录 `/login`；404 兜底。
- 员工端 `/employee/*`（`EmployeeLayout`）：`schedule`（我的班表）、`leave`、`swap`、`notifications`。
- 管理端 `/`（`MainLayout`）：`dashboard`、`employees`、`workstations`、`shift-templates`、`rules`、**`peak-hours` ★**、`schedules/generate`、`schedules/view`、`reports`、`audit-logs`、`leave-review`、`swap-review`、`notifications`。
- 路由守卫按角色分流（EMPLOYEE 只能进员工端；STORE_MANAGER/SYSTEM_ADMIN 进管理端）。

### 7.2 API 封装（`src/api/`）

`auth / employee(s) / leave / notifications / peakHours★ / rules / schedules / shiftTemplates / store / swap / workstations`，统一走 `utils/request`（axios 实例，401 自动清 token 跳登录）。

### 7.3 ★ 本次前端改动

- 新增「高峰时段」管理页（`PeakHoursView.vue` + `api/peakHours.js`），菜单与路由已挂。
- 日视图/月视图矩阵：班中休息员工灰色标记「休」，显示「顶班 XXX」或「无人顶岗」，支持跨午夜；月视图日历增加「N 人班中休」统计。
- 周视图甘特图：显示休息时段与顶岗人姓名。
- 员工端我的班表：新增「休息」列（含顶岗/无人顶岗提示）与「**我顶岗的记录**」区块（时间/顶岗岗位/替谁顶岗）。
- 发布排班问题列表已并入休息相关告警（`BREAK_UNCOVERED` / `BREAK_BORROW_INEXPERIENCED`）。

---

## 8. 本次未提交变更详解

### 8.1 班中休息（核心功能）

后端 `BreakAllocator`（见 §6.3）+ `ScheduleService` 生成时落库 4 个新列 + 月/周/日视图与 `/employee/my-schedule`（新增 `Covers` 数组）透出 + 前端展示（见 §7.3）+ 换班审批互换时同步复制休息字段 + 合理度扣减（§6.5）。

### 8.2 高峰禁休时段（配套配置）

新表 + CRUD 服务/接口 + 前端页面 + 算法接入（`SchedulingEngine` 读取门店启用时段传入 `SchedulingInput`）。

### 8.3 安全加固

- **JWT 绑定 password_version**：令牌携带 `password_version` claim，每次请求校验与用户当前版本一致；**改密码后旧令牌全部失效**（此前写死 "1"）。
- **登录限流按 IP + 账号双维度**：登录传入 clientIp，成功登录同时清除该 IP 与账号的失败计数/锁定（避免误伤共享出口 IP）。
- **OTP 验证码不再返回响应体**：非生产环境仅以 `[DEV-OTP]` 警告日志输出（短信网关接入前联调用）；生产环境不落日志、不进响应。
- **员工手机号脱敏**：列表与详情返回 `138****1234` 格式。
- **换班审批并发原子化**：改用 `ExecuteUpdate` 原子抢占 `PENDING` 状态，并发重复审批返回 `ALREADY_REVIEWED`，事务内互换排班。
- **通知查询补 StoreId 过滤**（工号 lookup 处）。

### 8.4 测试变化

新增 `BreakAllocatorTests`（3 个用例：短班不休息、高峰禁休窗口、借调顶岗等场景），更新 `SchedulingEngineTests`、`JwtTokenServiceTests`。**当前 14/14 通过**。

---

## 9. 接口增量速查（相对 docs/接口文档.md v1.0）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/peak-restricted-hours` | 高峰时段列表（AdminOnly） |
| POST | `/api/peak-restricted-hours` | 新增（HH:mm，30 分钟对齐） |
| PUT | `/api/peak-restricted-hours/{id}` | 修改/启停 |
| DELETE | `/api/peak-restricted-hours/{id}` | 删除 |

字段级变化（已上线接口的响应新增字段，向后兼容）：
- 月视图 `MonthDayCell`、周视图 `WeekDayShift`、日视图 `DailyViewItem`：新增 `BreakStartTime / BreakEndTime / BreakCoverEmployeeName`。
- `GET /employee/my-schedule`：`Plans[].Days[]` 新增休息字段；顶层新增 `Covers[]`（我顶岗他人的记录：日期/时间/岗位/替谁顶岗）。
- `GET /auth/me`、`POST /auth/login`：`CurrentUser` 新增 `PasswordVersion`。
- `POST /auth/send-reset-otp`：响应不再含 `DevOtp`，改为非生产环境日志输出。

---

## 10. 交接待办清单（建议顺序）

1. **（必做）提交并推送未提交改动**：建议拆分提交：① 数据库迁移 + 高峰时段 CRUD（前后端）② 班中休息算法 + 视图 + 员工端 ③ 安全加固（password_version/IP 限流/OTP/脱敏/并发审批）。推送前先 `git diff` 复核、跑一次 `dotnet test` 与前端构建。
2. **（必做）更新 `README.md`**：配置方式章节已过时（appsettings.Development.json 已删除，应改为 User Secrets / 环境变量说明）；补充班中休息、高峰时段功能；API 表补 `peak-restricted-hours` 与休息字段；账号表核对。
3. **（建议）更新 `docs/接口文档.md`**：新增高峰时段章节与 §9 字段级变化；版本号升 1.1。
4. **（建议）更新 `docs/功能清单.md`**：补「班中休息」「高峰禁休时段」「安全加固」条目；测试用例数 11 → 14。
5. **（小）`frontend/.env.example`** 的 `VITE_API_TARGET=http://localhost:5000` 与默认 5059 不一致，建议改 5059 或删注释说明。
6. **（小）根目录杂物清理**：`1`（1 字节）、`review-20260811.json` 等 4 个 0 字节 json、`scripts/` 空目录，可考虑清理或 gitignore。
7. **（待定）短信网关未接入**：OTP 目前仅日志输出（`[DEV-OTP]`），生产上线前必须接短信/邮箱通道，并把 `Program.cs` 中该日志分支在生产环境彻底关闭（现已按 `IsProduction()` 隔离）。

---

## 11. 已知问题与注意事项

- **BREAK_UNCOVERED 告警需人工**：高峰禁休、短班、借调不满足时算法只告警不强制解决，发布前店长需处理（发布页问题列表可见）。
- **高峰时段默认值**：算法无配置时兜底 20:00-22:00；门店配置被全部停用/删除后同样回退默认值。
- **休息照算工时**：员工周工时统计不受 30 分钟休息影响（产品决策，勿改）。
- **借调上限**：工作站借调与休息顶岗各自独立限制每人每天 2 次。
- **MySQL 版本**：代码声明 ServerVersion 8.0.36，本机 MySQL 9.6 实测正常；如遇 Pomelo 兼容问题，优先对齐 8.0。
- **`Program.cs` 是单文件 1800+ 行**：改端点建议按既有分区注释定位；后续可考虑迁移到控制器或按领域拆分，但当前勿做无谓重构。
- **多租户**：所有查询必须按 `StoreId` 过滤（本次已在通知工号查询处补漏）；换班审批使用原子 `ExecuteUpdate`，修改时保持。
- **`database/backups/` 含历史明文密码备份**（`shift_mvp_before_init_20260806_154731.sql` 中可见 `admin123_dev_plaintext_replace_later`），已被 .gitignore 排除，勿提交、勿外传。

---

## 12. 常用命令速查

```bash
# 后端
cd backend/ShiftScheduling.Api && dotnet run            # http://localhost:5059
cd backend/ShiftScheduling.Api.Tests && dotnet test     # 14 个用例
curl http://localhost:5059/api/health

# 前端
cd frontend && npm install && npm run dev               # http://localhost:5173
npm run build                                           # 产物 frontend/dist/

# 数据库
mysql -u root -p < database/init_shift_mvp.sql
mysql -u root -p shift_mvp < database/migrations/20260817_add_break_and_peak_hours.sql

# 配置（User Secrets）
cd backend/ShiftScheduling.Api
dotnet user-secrets list
dotnet user-secrets set "ConnectionStrings:ShiftMvp" "..."

# 登录接口验证
curl -X POST http://localhost:5059/api/auth/login -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"Admin@123456"}'
```

---

## 13. 历史里程碑（git log 摘要）

- `0eda671`（08-14）全职优先排班 + 补齐 E023 技能 —— **当前 HEAD**
- 08-14：兼职「兼」标记、行政岗 22:00 需求修正、空闲日休息记录修复、合理度图 ECharts 重写
- 08-13：新增 docs/功能清单.md、docs/接口文档.md；兼职员工 E101~E110 入库；Dashboard 33 人明细
- 08-12：低技能工作站标记
- 08-11：并发唯一约束、P2 数据模型修复、密码版本/规则版本
- 08-07：请假、换班、员工账号
- 08-06：init 基线（克隆即跑 + README 环境说明）

**2026-08-17（未提交）**：班中休息（阶段四）+ 高峰禁休时段 + 安全加固（本交接文档 §2/§8 详述）。

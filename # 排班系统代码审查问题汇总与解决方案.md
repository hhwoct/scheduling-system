这份报告是P2/P3问题的**补充**，包含了之前汇总中可能遗漏的细节问题。以下是所有新问题的完整汇总：

---

# 补充问题汇总

---

## 一、数据库迁移问题（新增）

### 问题1：迁移中硬编码store_id=1
- **位置**: `20260806_add_september_date_params.sql`
- **问题**: 只有门店1有日期参数，其他门店无法生成排班
- **修复**: 使用 `CROSS JOIN stores` 为所有门店生成

### 问题2：UPDATE无行数验证
- **位置**: `20260812_add_low_skill_workstation.sql`
- **问题**: 即使0行更新也显示成功
- **修复**: 检查 `ROW_COUNT()`，失败时回滚

### 问题3：密码哈希未验证
- **位置**: `20260811_fix_password_hashes.sql`
- **问题**: 硬编码哈希未验证是否匹配明文
- **修复**: 添加bcrypt验证脚本

### 问题4：迁移可重复执行造成破坏
- **位置**: `20260811_fix_password_hashes.sql`
- **问题**: 重新运行会覆盖已修改的密码
- **修复**: 只更新占位符行，添加幂等性检查

### 问题5：删除重复数据逻辑错误
- **位置**: `20260811_fix_p2_data_model.sql` 第59-60行
- **问题**: 注释说"保留最新"，但 `r1.id > r2.id` 删除了ID较大的（即**最新**的）
- **修复**: 改为 `r1.id < r2.id` 保留最新的

### 问题6：同一天既是工作日又是法定节假日
- **位置**: `20260811_fix_p2_data_model.sql` 第82行
- **问题**: 2026-09-27标记为 `WORKDAY` 但 `is_legal_holiday=1`（矛盾）
- **修复**: 从法定节假日列表中移除

### 问题7：国庆前夕未处理
- **位置**: `20260806_add_september_date_params.sql`
- **问题**: 9月30日是国庆前夕，但未标记为假日前夕
- **修复**: 添加 `WHEN d = '2026-09-30' THEN 1`

### 问题8：MySQL 8.0.19+语法兼容
- **位置**: `20260806_add_september_date_params.sql`
- **问题**: `AS new` 语法需要MySQL 8.0.19+
- **修复**: 使用 `VALUES()` 或版本检测

---

## 二、前端安全问题（新增）

### 问题9：员工身份从URL参数获取（IDOR）
- **位置**: `EmployeeScheduleView.vue`、`EmployeeLeaveView.vue`、`EmployeeSwapView.vue`
- **问题**: 从 `route.query.employeeNo` 获取员工号，无权限验证，任何用户可查看他人数据
- **修复**: 从认证会话获取，仅管理员可覆盖

### 问题10：菜单权限缺失
- **位置**: `MainLayout.vue`
- **问题**: 所有管理菜单对任何登录用户显示
- **修复**: 根据 `authStore.user.role` 过滤菜单项

### 问题11：前端角色依赖localStorage
- **位置**: `EmployeeLayout.vue`
- **问题**: 用 `localStorage.getItem('shift_role')` 做权限判断，可被篡改
- **修复**: 等待 `authStore.fetchCurrentUser()` 后使用服务端角色

### 问题12：LoginView预填充admin
- **位置**: `LoginView.vue`
- **问题**: 用户名预填 `admin`，便于暴力破解
- **修复**: 移除默认值

---

## 三、前端错误处理问题（新增）

### 问题13：try/finally 无 catch
- **位置**: 多个视图
- **问题**: API失败变成未处理拒绝
- **修复**: 添加 `catch` 显示错误消息

### 问题14：请求无序列号/取消
- **位置**: 多个视图
- **问题**: 旧响应覆盖新数据
- **修复**: 使用 `requestSeq` 忽略过期响应

### 问题15：ElMessageBox取消未处理
- **位置**: `ScheduleGenerateView.vue`、`EmployeeView.vue`
- **问题**: 取消确认弹窗产生未处理拒绝
- **修复**: 用 `try/catch` 捕获取消

### 问题16：刷新失败误报为操作失败
- **位置**: `LeaveReviewView.vue`
- **问题**: 审批成功但刷新失败，提示"操作失败"
- **修复**: 将刷新移出操作try/catch

### 问题17：错误消息显示 `undefined`
- **位置**: `EmployeeScheduleView.vue`
- **问题**: 假设错误有 `message` 属性
- **修复**: 标准化错误对象

### 问题18：请求序号未清理
- **位置**: `AuditLogView.vue`
- **问题**: 组件卸载后旧响应仍修改状态
- **修复**: 在 `onUnmounted` 中递增序号

### 问题19：定时器泄漏
- **位置**: `EmployeeLayout.vue`
- **问题**: 定时器在异步函数中赋值，`onUnmounted` 可能为null
- **修复**: 同步设置定时器

---

## 四、前端数据一致性（新增）

### 问题20：日期选择器清空无验证
- **位置**: `EmployeeScheduleView.vue`
- **问题**: `month.value` 为null时发送请求
- **修复**: 添加前端验证

### 问题21：逾期日期仍可选
- **位置**: `EmployeeSwapView.vue`
- **问题**: `TODAY0` 在setup时计算，跨午夜后过期
- **修复**: 每次调用时动态计算

### 问题22：query变化不刷新
- **位置**: `NotificationView.vue`
- **问题**: `employeeNo` 变化时不刷新列表
- **修复**: `watch(() => route.query.employeeNo, loadData)`

### 问题23：删除最后一条记录页码错误
- **位置**: `EmployeeView.vue`
- **问题**: 删除当前页唯一记录后，页面为空
- **修复**: 如果列表为空且page>1，page减1

### 问题24：弹窗验证状态残留
- **位置**: `EmployeeView.vue`
- **问题**: 失败验证错误在重新打开弹窗时仍显示
- **修复**: 填充表单后调用 `clearValidate()`

### 问题25：预览模式提交归属错误
- **位置**: `EmployeeLeaveView.vue`
- **问题**: 预览其他员工时，提交请假归属登录用户
- **修复**: 提交时使用 `employeeNo` 或禁用预览模式提交

### 问题26：空分页显示
- **位置**: `NotificationView.vue`
- **问题**: 数据为空时仍显示分页组件
- **修复**: `v-if="total > 0"`

### 问题27：甘特图凌晨班次不显示
- **位置**: `ShiftTemplateView.vue`
- **问题**: 00:00-04:00的班次被隐藏
- **修复**: 凌晨班次整体偏移24小时到次日

### 问题28：凌晨跨天缺口不显示
- **位置**: `DailyScheduleView.vue`
- **问题**: 00:00-03:00 '+1'槽位的缺口不显示
- **修复**: 匹配时使用 `targetDate = slot.isNextDay ? nextDate : workDate`

### 问题29：无员工的缺口岗位不显示
- **位置**: `DailyScheduleView.vue`
- **问题**: 只有缺口的岗位不出现在矩阵中
- **修复**: 从缺口问题中添加岗位名

### 问题30：表单无校验
- **位置**: `ShiftTemplateView.vue`
- **问题**: 名称/时间为空时可提交
- **修复**: 添加 `el-form` 的 `ref` + `rules` + `validate()`

---

## 五、前端逻辑错误（新增）

### 问题31：周视图回退到错误日期
- **位置**: `ScheduleView.vue`
- **问题**: 只取第一页计划，目标计划不在第一页时回退到今天
- **修复**: 从已加载的 `plans.value` 中查找

### 问题32：缺口问题跨周显示
- **位置**: `ScheduleView.vue`
- **问题**: 整个计划的缺口问题在每周都显示
- **修复**: 用当前周日期集合过滤

### 问题33：日期未选择无提示
- **位置**: `ScheduleView.vue`
- **问题**: 清空日期点击查询无反馈
- **修复**: 设置 `errorMsg`

### 问题34：其他扇区点击清空表格
- **位置**: `ScheduleView.vue`
- **问题**: 点击饼图"其他"扇区，`filterTableByWs('其他')` 清空表格
- **修复**: 忽略"其他"的点击

### 问题35：规则版本号未更新
- **位置**: `RuleConfigView.vue`
- **问题**: 保存后本地仍使用旧版本号
- **修复**: 从响应中更新 `version`

### 问题36：部分失败只显示数量
- **位置**: `RuleConfigView.vue`
- **问题**: 不显示哪些规则失败
- **修复**: 显示失败的规则名

### 问题37：`dayIssues` 只返回连续工作
- **位置**: `WeekScheduleView.vue`
- **问题**: OVERTIME分支死代码
- **修复**: 包含OVERTIME类型

### 问题38：空结果缓存阻止重试
- **位置**: `WeekScheduleView.vue`
- **问题**: `[]` 缓存后，`if (cache[key])` 阻止重试
- **修复**: 跟踪加载状态而非用length判断

### 问题39：死代码残留
- **位置**: `ScheduleView.vue`
- **问题**: `dailyBars`、`wsPieLabels`、`isFilterActive` 定义但未使用
- **修复**: 删除

### 问题40：`getScheduleIssues` 重复请求
- **位置**: `MonthScheduleView.vue`
- **问题**: 每次点击日期都重新请求，可缓存
- **修复**: 缓存每个planId的结果

---

## 六、后端安全/权限问题（新增）

### 问题41：管理端点无角色策略
- **位置**: `Program.cs` 第212-215行
- **问题**: 所有管理端点只用 `RequireAuthorization()`，任何认证用户可操作
- **修复**: 添加 `AdminOnly` 策略

### 问题42：JWT密码版本号硬编码
- **位置**: `JwtTokenService.cs`
- **问题**: 始终生成 `"password_version": "1"`，密码重置后旧令牌仍有效
- **修复**: 从用户实体读取真实版本号

### 问题43：`OnTokenValidated` 不验证密码版本
- **位置**: `Program.cs` 第90-93行
- **问题**: 只检查用户存在和状态，不比较版本号
- **修复**: 验证JWT中的版本号与用户记录一致

### 问题44：JWT泄露PII
- **位置**: `JwtTokenService.cs`
- **问题**: username/nickname/store_id在未加密的JWT载荷中
- **修复**: 只存储必要的标识符

### 问题45：登录端点无限流
- **位置**: `Program.cs` 第141行
- **问题**: 无速率限制，可暴力破解
- **修复**: 添加 `[EnableRateLimiting]`

### 问题46：SigningKey空值处理
- **位置**: `Program.cs` 第50行
- **问题**: 空key导致 `ArgumentNullException`
- **修复**: 先检查 `string.IsNullOrWhiteSpace`

### 问题47：operatorUserId默认0
- **位置**: `Program.cs` 第215-216行
- **问题**: `?? 0` 写入错误ID
- **修复**: 在认证上下文中确保ID存在

---

## 七、Git忽略问题（新增）

### 问题48：环境配置未完整忽略
- **位置**: `.gitignore`
- **问题**: 只忽略Production，Staging/UAT可能被提交
- **修复**: `appsettings*.json` + `!appsettings.json` + `!appsettings.Development.json`

### 问题49：`.env` 位置错误
- **位置**: `.gitignore`
- **问题**: 放在"日志"章节下
- **修复**: 移到"敏感配置"章节

### 问题50：重复规则
- **位置**: `.gitignore`
- **问题**: `database/backups/` 出现两次
- **修复**: 保留一处

---

## 修复优先级汇总

| 优先级 | 数量 | 类型 |
|--------|------|------|
| **P0** | 10 | 安全漏洞（IDOR、无权限、JWT失效） |
| **P1** | 12 | 数据完整性（迁移错误、版本号） |
| **P2** | 18 | 前端错误处理 |
| **P3** | 10 | 代码清理和优化 |
